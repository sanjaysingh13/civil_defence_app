services:
  docs:
    image: civil_defence_app_local_docs
    container_name: civil_defence_app_local_docs
    build:
      context: .
      dockerfile: ./compose/local/docs/Dockerfile
    env_file:
      - ./.envs/.local/.django
    volumes:
      - /app/.venv
      - ./docs:/docs:z
      - ./config:/app/config:z
      - ./civil_defence_app:/app/civil_defence_app:z
    ports:
      - '9000:9000'
    command: /start-docs
